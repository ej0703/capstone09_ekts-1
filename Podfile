# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'pushNotification_Firebase' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
pod 'Firebase/Core'
pod 'Firebase/Messaging'
  # Pods for pushNotification_Firebase

  target 'pushNotification_FirebaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'pushNotification_FirebaseUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
